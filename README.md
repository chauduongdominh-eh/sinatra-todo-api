## Dev env setup

Install dependencies:

```sh
bundle install
```

Build Yard docs for Solargraph:

```sh
yard gems
```

Start the server:

```sh
rackup
```

By default, the server runs on port `9292`
